# M2.3 Review Round B — Dimension Scorecard and Submodule Gate

## Preconditions

Round A ran without scores and found 0 Critical, 9 Major and 3 Minor issues. Every finding was root-fixed with no waiver before this scorecard. The completed diff was then revalidated in an isolated Python 3.11 environment.

## Scorecard

| Dimension | Score (0–5) | Evidence | Remaining issue |
|---|---:|---|---|
| Correctness | 5 | 111 full-suite tests; 17 dedicated Research Runtime tests; stable orientation/targeted plans; cycle/outline identity; tamper and negative cases | None known in M2.3 scope |
| Architecture consistency | 5 | Research Result/Evidence separation; provider-neutral `ResearchProvider`; runtime schemas are non-catalog facts; ADR-0011; no Source/Evidence write bypass | M2.4 must preserve the materialization boundary |
| Cache and lineage integrity | 5 | provider/query/freshness/source-tier/result-limit/TTL input identity; immutable content-addressed snapshots; self-validating key/expiry/result IDs; generation invalidation | Historical cache GC remains future operational work |
| Recovery and failure semantics | 5 | per-task checkpoint; partial/failed resume; orphan cache reuse; invalidation ordering reconciliation; read-only workspace validation | Multi-process stress beyond file-lock contract remains future CI expansion |
| Security and resource safety | 5 | bounded queries/results/metadata; bounded iterator consumption; HTTP(S)-only result URLs; JSON metadata; offline fail-closed; no Web fetch/execution in runtime | Production vendor adapters require separate security review |
| Capability truthfulness | 5 | Run `complete` explicitly means execution only; raw Research Results cannot pass G2/G5A or become Evidence; offline returns no fabricated results | Evidence adjudication intentionally waits for M2.4 |
| Testability | 5 | deterministic fake providers/clocks; no unit-test network; provider failure, stale cache, drift, malformed marker, unsafe URL, stream overflow, tampering and CLI coverage | Broader cross-platform lock matrix remains future CI work |
| Maintainability | 4 | typed DTOs, one runtime service, two bounded runtime schemas, explicit CLI/inspection helpers, package-audit coverage | `services/research.py` is now substantial; split planner/cache/executor internals if M2.4+ adds more responsibilities rather than extending it indefinitely |

## Severity Gate

- Critical open issues: 0.
- Major open issues: 0.
- Minor blocking issues: 0.
- Waivers used: none.
- Full M2 Exit Gate claimed: no.

## Final verification

Executed in an isolated `python:3.11-slim` copy with development/ingestion dependencies and Git installed:

```text
python -m compileall -q src tests scripts
PASS

ruff check --no-cache src tests scripts
All checks passed!

pytest -o addopts=""
111 passed in 54.50s

python scripts/validate_all.py
PASS: 16 schemas, example workspace, G0-G6, G7 negative control, and 3 wireframes

python scripts/audit_package.py
PASS: 18/18 checks; 248 files hashed

git diff --check
PASS
```

The package audit now validates every repository `*.schema.json`, including the non-catalog Research Run and Research Cache schemas, while preserving the 16 semantic catalog artifacts.

## Submodule Gate

**M2.3 Research Planning/Runtime: PASS.**

M2.3 now provides deterministic two-pass planning, provider/version lineage, bounded resumable task execution, immutable query-result snapshots, TTL and generation invalidation, provider/cache failure checkpointing, offline blocking, runtime tamper validation and operator inspection without treating search output as Evidence.

This authorizes M2.4 Evidence Engine. It does not complete result-to-source materialization, claim normalization/deduplication, conflict/freshness/authority adjudication, block-level evidence binding, the M2-wide Exit Gate or v0.5 Planning Pipeline.
