interface:
  display_name: "Slidethus Render"
  short_description: "Render admitted designs into inspectable candidates"
  default_prompt: "Use $slidethus-render to produce a PPTX candidate from this workspace and report actual export checks and limitations."
policy:
  allow_implicit_invocation: true
