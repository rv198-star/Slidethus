# v0.8.1 后续问题记录

这是当前 0.x 能力发布披露的剩余边界，不恢复历史撤回代码，不授权本轮扩大实现。

## V081-FU-001｜M3 审阅返工目标与合法状态迁移一致性

- 状态：待处理；自动恢复路径存在限制，不是已修复项。
- 观察：Planning Review 要求回到 P3 时，M3 应用尝试 `LAYOUT_READY -> NARRATIVE_READY`，但状态机不允许该直达边。
- 基线：v0.8.0 同样存在这段直达调用；v0.8.1 未修改 `m3_application.py` 或 `state_machine.py`。
- 通用范围：最早责任阶段、合法返工路径、下游失效与失败报告，不使用行业、标题或页码分支。
- 临时边界：保留当前产物和失败记录，正式重入受影响阶段；不可手动伪造 Gate，不能声称自动恢复成功。
- 后续验收：用合成 planning finding 覆盖跨阶段返工、无效目标及中断恢复；验证目标阶段、失效范围与 report 一致，现有合法单跳路径不回归。

## V081-FU-002｜恢复时 Brief 元数据与语义变更的失效边界

- 状态：待复现和定级；尚不认定框架缺陷。
- 观察：恢复时未沿用原 hints，Brief completion 元数据变化导致旧规划 lineage 失效，需重新提交绑定新上下文的响应。
- 后续验收：分别验证同 hints 幂等恢复、仅 completion 元数据变化、真实 Brief 语义变化；明确哪些变化应保留或失效规划，禁止复用 stale 响应绕过校验。

## 案例验收待办（非新增能力）

YU7 原始候选的 PowerPoint 打开、无修复提示和逐页检查仍未完成。用户认可产出不被推定为这些具体检查的证据；后续结果应绑定候选文件哈希。
