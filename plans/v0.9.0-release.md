# v0.9.0 Pre-release 收尾

## 1. Objective

- 用户授权按 0.9.0 发布，但明确尚未实测验收；本轮只做简单发布收尾。
- GitHub 标记 Pre-release，保留 v0.8.1 为 latest；不上传 PyPI，不更新全局技能，不启动新案例、Office 验收、多 Python 审计或框架优化。
- 退出条件：版本一致、分发资源完整、安装验证通过，提交/标签/Release 附件可核对；不冒充已完成审美验收。

## 2. Current state

- main 恢复点 `e5940891659e128aabe93a04b12038d9086add3c` 已推送；剩余 36 套已在本地补齐，44 套参考实现见 `plans/design-reference-library.md`。
- 刚完成的同一功能基线：Python 3.11.11 全量 377 passed / 43 skipped / 0 failed（750.18s），日志 `/tmp/slidethus-design-44-pytest-20260831.log`；32 项资源/分发测试、校验与实际离线安装通过。
- 发布差异仅为包版本、README 和发布记录；不重跑刚结束的长时全量测试，复用明确基线，补跑版本/资源/分发测试及新版本安装校验。

## 3. Decisions and boundaries

- 44 套全量收录不等于每次全读：自主索引 → 少量适配卡 → 必要预览，允许 none/reuse/局部借鉴。
- Taste 仍为默认原生视觉原型基础；参考资源不是新 provider、行业模板或审美质量证明。
- 本轮 src 仅版本常量变化，Schema/renderer/Taste 原文不改；无新依赖。
- M6.6 / v1.0 未通过、既有 M3 返工限制及 Office 验收缺口继续披露。

## 4. Work breakdown

| Step | 产出与验证 | 状态 |
|---|---|---|
| 1 | 版本、README、候选发布说明与待实测清单 | complete |
| 2 | 单 Python 定向回归、Wheel/ZIP、独立安装与哈希 | complete |
| 3 | 提交推送、v0.9.0 标签、Pre-release 与附件 | 发布交接；远端回执见 Issue #1 |

## 5. Verification

```bash
.venv/bin/python -m pytest tests/test_design_references.py tests/test_skill_layout.py tests/test_distribution.py
.venv/bin/python scripts/validate_all.py
.venv/bin/python scripts/audit_package.py
.venv/bin/python -m compileall -q src tests scripts
.venv/bin/ruff check src tests scripts
```

构建使用现有缓存和 Python 3.11，不进行多版本审计：

- 新版本专项测试：32 passed / 2.58s；validate_all、compileall、Ruff 与许可 7/7 通过。包审计在最终文档冻结后刷新，回执见 `audit/automated-audit.md`。
- 全量测试复用本轮紧邻的功能基线 377 passed / 43 skipped；不是重新执行一轮全量测试。
- 实际 Wheel 在仓库外 Python 3.11.11 环境离线安装：版本 0.9.0，162 个 Python/Schema 文件、95 个分发资源、9 个技能、44 张卡逐字节匹配；安装幂等、链接闭合、从安装包再构建 Plugin/SBOM 通过。
- 正式 Plugin ZIP 采用上述干净安装环境的构建结果。源码环境与 Wheel 元数据的依赖区间排序不同（例如 `>=4.22,<5` 与 `<5,>=4.22`），仅导致 SBOM 及其清单哈希不同；其余 ZIP 内容逐字节一致。本轮不为元数据排序增加运行代码变更。
- 验证工作目录：`/tmp/slidethus-v090.kK9DUh`；未覆盖旧版发布资产或全局技能。

候选附件目录 `dist/releases/v0.9.0/`：

| 附件 | SHA-256 |
|---|---|
| slidethus-0.9.0-py3-none-any.whl | `484665c2307474e9dd0c39736a52986f9a90b9c3432cf2ef2ffad4134f278da5` |
| slidethus-plugin-v0.9.0.zip | `c367a7d954023b7efcacb07d45bb8397eaac31d08f23e3f2f92ee527cabd113c` |

随附件发布 `SHA256SUMS`；上传后核对 tag、Pre-release 标志、附件摘要及 v0.8.1 latest 状态。

## 6. Review and deferred acceptance

- 只检查发布差异、许可/来源、分发一致性及真实声明，不另开审计轮次。
- 待晚点实测：自主选择是否合理（包括不选/复用）、Taste 原生样板是否贯穿正式流程、图像/数值图表按内容兑现、全篇配色/构图/密度节奏、PowerPoint 无修复提示与全篇真实视觉审阅。
- 样例/场景由后续真实任务确定，不现在扩展行业集合；实测发现问题后先归因，再决定通用修复。

## 7. Outcome

本地发布候选完成；远端提交/标签/附件回执在发布后记录到 [Issue #1](https://github.com/rv198-star/Slidethus/issues/1)，避免把尚未完成的外部动作写成已完成。发布目标为 [v0.9.0 Pre-release](https://github.com/rv198-star/Slidethus/releases/tag/v0.9.0)；预发布不意味着正式审美验收，Issue 保留待实测事项。
